@{
    ViewData["Title"] = "Daily Forecast";
}

@model DailyForecastDataObjects

<div class="text-center">
    <h1 class="display-4">Your @Model.cnt day forecast:</h1>
    <img src=@Model.iconURI alt="Weather icon" />
    @if (Model.list is not null)
    {
        @foreach (var forecast in Model.list)
        {
            <p>Test</p>
        }
    }

</div>